[project]
name = "publix-sorter"
version = "0.3.0"
description = "Sort a grocery list using Publix locations"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
publix-sorter = "publix_sorter.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/publix_sorter"]
